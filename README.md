Running docker-compose up is going to start the system.
The server creates the DB when starting to run, so it might take some time.
In order to create the DB, you must have a folder called imbd_files inside this directory.
imdb_files must include files called: title.principals.tsv, name.basics.tsv
